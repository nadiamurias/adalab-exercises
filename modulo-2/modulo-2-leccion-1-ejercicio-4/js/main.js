"use strict";
debugger;
const titleElement = document.querySelector(".js-title");
const firstNameElement = document.querySelector(".js-firstName");
const secondNameElement = document.querySelector(".js-secondName");

let result = titleElement.innerHTML + firstNameElement.innerHTML;

//incluir el nomnbre de la segunda
//result = titleElement.innerHTML + secondNameElement.innerHTML;

console.log(`La Adalaber seleccionada es: ${firstNameValue}`);
