'use strict';

const access = 'Nadia';

if (access === 'Sara'|| access === 'Nadia'){
console.log(`Bienvenida, ${access}`)
}
else {
    console.log('Lo siento pero el usuario que has introducido no está registrado');
}
